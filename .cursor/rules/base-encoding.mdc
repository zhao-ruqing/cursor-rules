---
alwaysApply: true
description: 全局强制UTF-8无BOM编码，杜绝中文注释GBK乱码
alwaysApply: true
globs: ["**/*.vue","**/*.ts","**/*.tsx","**/*.js","**/*.jsx","**/*.json","**/*.md"]
priority: highest
---

# 编码强制约束（最高优先级）

# 1. 对话语言硬性约束（全局最高优先级）

1. 全程只用标准简体中文进行对话回复，禁止英文、繁体、混杂外文沟通；
2. 代码内标识符、变量、函数名使用英文，仅注释、沟通文字使用简体中文；
3. 解释、方案、报错说明全部中文，不输出纯英文无翻译内容。

# 2. 编码强制约束

1. AI新增、修改、重写任何代码文件时，统一使用 UTF-8 无BOM 格式写入，禁止GBK/ANSI/GB2312编码。
2. 所有单行//、多行/\* \*/注释必须输出标准简体UTF-8中文，严禁生成乱码字符（如��ȡ、方块问号、残缺字符）。
3. 若识别到原文件为GBK编码，生成中文注释时自动做UTF8兼容转换，避免编码冲突乱码。
4. 禁止使用文件覆盖操作篡改原有文件编码，修改代码优先增量编辑。
5. 输出代码后若出现中文乱码，主动给出修复步骤：右下角切换Reopen with GBK → Save with UTF-8。
