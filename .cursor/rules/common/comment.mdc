---
alwaysApply: true
description: 统一注释书写规范，强制函数/工具添加单行说明
alwaysApply: true
globs: ["**/*.vue","**/*.ts","**/*.js","**/*.utils.ts","**/*.composables.ts"]
priority: high
---

# 注释硬性规范

1. 每新增函数、方法、工具类utils、组合式函数useXxx，上方必须添加一行简短单行注释，示例：`// 获取用户列表数据`。
2. 单行注释`//`统一写在代码块上方，行尾注释仅用于极简短标记，不写长描述。
3. 工具函数、复杂逻辑、接口请求方法必须补充中文JSDoc，说明入参、返回值、业务用途。
4. 注释语句通顺简洁，只使用简体中文，不夹杂无意义符号、英文乱码。
5. 禁止冗余、重复、无意义注释，只写业务逻辑说明，不重复复述代码本身。
6. 大段业务逻辑、分页、弹窗、权限校验等模块，头部增加单行注释区分模块作用。
